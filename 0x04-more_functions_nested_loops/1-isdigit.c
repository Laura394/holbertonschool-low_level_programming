#include "holberton.h"
/**
* _isdigit - checks for a digit.
*@c: is a variable
*Return: Always 0.
 */
int _isdigit(int c)
{

	if (c == '0')
	{
		return (1);
	}
	else
	{
		return (0);
	}

	return (0);
}
